# CC Orchestrator Environment Variables
# Copy to .env and fill in actual values

# === Host Paths (REQUIRED — must be absolute paths) ===
# Projects directory — where all project code lives on the host
HOST_PROJECTS_DIR=/home/YOUR_USERNAME/cc-projects

# === Auth (choose one) ===
# Option A: API key
# ANTHROPIC_API_KEY=
# Option B: Claude Max OAuth — set HOST paths to your ~/.claude/ directory and ~/.claude.json
HOST_CLAUDE_DIR=/home/YOUR_USERNAME/.claude
HOST_CLAUDE_JSON=/home/YOUR_USERNAME/.claude.json
HOST_USER_UID=1000

# === Optional (needed for voice input) ===
OPENAI_API_KEY=

# === Worker Config ===
MAX_CONCURRENT_WORKERS=5
MAX_IDLE_AGENTS=20
WORKER_CPU_LIMIT=2
WORKER_MEM_LIMIT=4g
TASK_TIMEOUT_SECONDS=600
MAX_RETRIES=3
CC_MODEL=claude-sonnet-4-5-20250514

# === Plan Mode ===
# AUTO_APPROVE_TIMEOUT=300  # Uncomment to enable auto-approve (seconds)

# === Backup ===
BACKUP_INTERVAL_HOURS=1
MAX_BACKUPS=48

# === Server Ports ===
PORT=8080
FRONTEND_PORT=3000
